# Docker Compose — local development defaults
# Copy to .env in this directory: cp .env.example .env

POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=dsa_studio
POSTGRES_PORT=5432

REDIS_PORT=6379
